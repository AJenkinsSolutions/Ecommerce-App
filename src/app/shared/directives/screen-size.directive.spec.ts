import { ScreenSizeDirective } from './screen-size.directive';

describe('ScreenSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenSizeDirective();
    expect(directive).toBeTruthy();
  });
});
